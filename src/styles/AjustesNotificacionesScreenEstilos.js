import { StyleSheet } from 'react-native';

// Estilos de Notificaciones - Sección descriptiva superior
export const notificacionesEstilos = (colores) => StyleSheet.create({
  descripcionSeccion: {
    fontSize: 13,
    lineHeight: 20,
    marginBottom: 20,
  },
});
